plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  "tag:Name": "web"
  "tag:Role": "Devops"

hostnames:
  - ip-address    # use public IP

keyed_groups:
  - key: tags.Role
    prefix: role
  - key: tags.Name
    prefix: name
